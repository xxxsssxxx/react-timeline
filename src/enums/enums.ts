export enum EPriority {
  VERY_HIGHT = "very_hight",
  HIGHT = "hight",
  MEDIUM = "medium",
  LOW = "low"
};

export enum ESide {
  RIGHT = "right",
  LEFT = "left"
}

export enum EType {
  NUMERIC = "numeric",
  TIMING = "timing"
}